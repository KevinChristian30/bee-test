@using BeeTest.Pages.Components.QuestionForms

@if (@QuestionType == "Multiple Choice")
{
    <MultipleChoiceQuestionForm TestId="@TestId" />
}
else if (@QuestionType == "Boolean")
{
    <BooleanQuestionForm TestId="@TestId" />
}
else if (@QuestionType == "Checkboxes")
{
    <CheckboxesQuestionForm TestId="@TestId" />
}
else if (@QuestionType == "Essay")
{
    <EssayQuestionForm TestId="@TestId" />
}
else if (@QuestionType == "File")
{
    <FileQuestionForm TestId="@TestId" />
}

@code {
    [Parameter]
    public string QuestionType { get; set; }

    [Parameter]
    public int TestId { get; set; }
}